{
  "builds": [
    {
      "src": "api/index.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "api/(.*)",
      "dest": "api/index.js"
    }
  ],
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}
